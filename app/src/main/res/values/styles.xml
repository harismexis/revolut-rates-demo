<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BaseTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/base_text_size</item>
    </style>

    <style name="RateItemKeyTextView" parent="RateItemTextView">
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/responder_item_key_text_size</item>
    </style>

    <style name="RateItemTextView" parent="BaseTextView">
        <item name="android:textColor">@color/light_grey</item>
        <item name="android:padding">@dimen/responder_item_text_view_padding</item>
    </style>

    <style name="ProgressbarStyle.Dark" parent="Widget.AppCompat.ProgressBar">
        <item name="colorAccent">@color/colorPrimaryDark</item>
    </style>

    <style name="EditTextTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/grey_three</item>
        <item name="colorControlActivated">@color/blue</item>
        <item name="colorControlHighlight">@color/blue</item>

    </style>

</resources>